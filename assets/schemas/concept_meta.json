{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "concept_meta.json",
  "title": "Concept Frontmatter",
  "description": "Schema for the YAML frontmatter in every <concepts_dir>/<id>/<id>.md file. Content only — progress state lives in .state/_progress.json.",
  "type": "object",
  "required": ["id", "name", "description", "related_concepts", "tags", "sources"],
  "additionalProperties": false,
  "x-body-sections": {
    "description": "Markdown body sections expected in every concept file. Not frontmatter — extracted by ingest, never validated by JSON Schema.",
    "sections": {
      "## Key Takeaways": {
        "description": "Bullet list of the most important things to understand about this concept.",
        "format": "bullet-list",
        "ingest": "append new bullets; never duplicate"
      },
      "## From sources": {
        "description": "One line per source wikilink that contributed to this concept, describing what it added.",
        "format": "bullet-list",
        "ingest": "append new entries; never duplicate"
      },
      "## Notes": {
        "description": "Free-form overflow notes that don't fit a first-class section.",
        "format": "freeform-markdown",
        "ingest": "append paragraphs; preserve existing text"
      }
    }
  },
  "properties": {
    "id": {
      "type": "string",
      "description": "Snake_case ID matching the folder name under concepts_dir and the entry in index.md",
      "pattern": "^[a-z0-9]+(_[a-z0-9]+)*$"
    },
    "name": {
      "type": "string",
      "description": "Human-readable concept name"
    },
    "description": {
      "type": "string",
      "description": "One-sentence plain-English summary of what this concept is"
    },
    "related_concepts": {
      "type": "array",
      "description": "[[wikilink]] references to closely related concept IDs — prerequisites, follow-ons, or siblings",
      "items": { "type": "string" },
      "uniqueItems": true
    },
    "tags": {
      "type": "array",
      "description": "Free-form keywords for cross-cutting themes",
      "items": { "type": "string" },
      "uniqueItems": true
    },
    "sources": {
      "type": "array",
      "description": "[[wikilink]] references to source nodes that have informed this concept",
      "items": { "type": "string" },
      "uniqueItems": true
    },
    "difficulty": {
      "type": "string",
      "description": "Difficulty level of this concept relative to the curriculum",
      "enum": ["beginner", "intermediate", "advanced", "expert"]
    },
    "workflows": {
      "type": "array",
      "description": "[[wikilink]] references to workflow nodes that list this concept as a prerequisite. Optional — populated when a workflow is created that gates on this concept.",
      "items": { "type": "string" },
      "uniqueItems": true
    }
  }
}
