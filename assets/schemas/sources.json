{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sources.json",
  "title": "Learning Sources",
  "description": "Schema for sources.json — registry of external resources mapped to curriculum concepts",
  "type": "array",
  "items": { "$ref": "#/definitions/source_entry" },
  "definitions": {
    "source_entry": {
      "type": "object",
      "required": ["url"],
      "additionalProperties": false,
      "properties": {
        "url": {
          "type": "string",
          "description": "Canonical URL of the external resource",
          "format": "uri"
        },
        "title": {
          "type": "string",
          "description": "Human-readable title of the resource"
        },
        "description": {
          "type": "string",
          "description": "One-to-two sentence summary of what this resource covers and why it is useful"
        },
        "concepts": {
          "type": "array",
          "description": "Snake_case concept IDs from concepts.json that this resource maps to",
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9]+(_[a-z0-9]+)*$"
          },
          "uniqueItems": true
        }
      }
    }
  }
}
