## TODO

- [x] Add `idea` node
- [ ] Add collection/sources, A `source` node that provides multiple data (in terms of our nodes) in this case it should parse all the data and combine with our wiki.
- [ ] Add `protocol` node: examples rest, graphql, rss, stix, udp, tcp. etc
- [x] In `tool` node, url and source should not be duplicate.
- [x] Add `collection` prop for every node with the node name, so that obsidian like apps filter. with this prop.